#include <iostream>
#include <utility>
#include <vector>

using namespace std;

int main ()
{
    /* Pair used to keep heterogeneous data types together
    pair <int, string> testPair (100, "TestPair");

    cout << testPair.first << endl;
    cout << testPair.second << endl;*/

    /* Vector is just like contigious array
    vector <int> vec;
    vector <int> :: iterator i;
    vector <int> :: reverse_iterator ri;

    for ( int i = 1; i <= 5 ; i++ )
    {
        vec.push_back(i);
    }

    for ( i = vec.begin(); i != vec.end(); i++ )
    {
        cout << *i;
    }

    for ( ri = vec.rbegin(); ri != vec.rend(); ri++ )
    {
        cout << *ri;
    }*/

    
}